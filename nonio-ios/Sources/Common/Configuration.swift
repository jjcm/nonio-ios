import Foundation

struct Configuration {
    static let API_HOST = URL(string: "https://api.non.io")!
    static let IMAGE_API_HOST = URL(string: "https://image.non.io")!
    static let VIDEO_API_HOST = URL(string: "https://video.non.io")!
    static let WEB_SOCKET_HOST = URL(string: "wss://video.non.io")!
}
