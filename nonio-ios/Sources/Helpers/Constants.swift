import Foundation

struct Constants {
    static let ImageBaseURL = URL(string: "https://image.non.io")!
    static let ThumbnailBaseURL = URL(string: "https://thumbnail.non.io")!
    static let AvatarBaseURL = URL(string: "https://avatar.non.io")!
    static let VideoBaseURL = URL(string: "https://video.non.io")!
}
